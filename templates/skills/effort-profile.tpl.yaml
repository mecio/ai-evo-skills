version: "1.0"
name: <namespace>-<profile-name>
description: "TODO: describe the resource and output policy."
reasoning: {level: medium}
resources: {subagents: disabled, network: disabled}
execution: {concurrent-on-same-worktree: false, reuse-session: correction-only}
output: {delivery: final-only, verbose-logs: summarize}
tests: {strategy: necessary, repeat-passed: false}
context: {repeat-readable-content: false, repository-exploration: targeted}
report: {profile-application: summary}
