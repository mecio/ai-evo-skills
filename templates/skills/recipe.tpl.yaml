version: "1.0"
name: <namespace>-recipe-<skill-name>

inputs: {}

steps:
  - id: TODO
    uses: <namespace>-TODO
    # Optional executor for this call; omit to use the current AI:
    # executor: claude
    # Optional exact comparison; references must name an input or an earlier step:
    # when:
    #   value: "${{ inputs.context }}"
    #   equals: "php83"
    #   normalize: trim  # Optional: ignore outer whitespace in the compared value only.

outputs:
  result:
    value: "${{ steps.TODO.output }}"
